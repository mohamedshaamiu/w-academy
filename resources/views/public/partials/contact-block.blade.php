<section class="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8">
    <h2 class="text-xl font-bold text-navy">{{ __('public.contact.title') }}</h2>

    <dl class="mt-4 grid grid-cols-1 gap-4 sm:grid-cols-3">
        <div>
            <dt class="text-sm font-semibold text-navy-400">{{ __('public.contact.address_heading') }}</dt>
            <dd class="mt-1 text-navy-700">{{ __('public.contact.address_value') }}</dd>
        </div>
        <div>
            <dt class="text-sm font-semibold text-navy-400">{{ __('public.contact.phone_heading') }}</dt>
            <dd class="mt-1 text-navy-700">{{ __('public.contact.phone_value') }}</dd>
        </div>
        <div>
            <dt class="text-sm font-semibold text-navy-400">{{ __('public.contact.office_hours_heading') }}</dt>
            <dd class="mt-1 text-navy-700">{{ __('public.contact.office_hours_value') }}</dd>
        </div>
    </dl>
</section>
