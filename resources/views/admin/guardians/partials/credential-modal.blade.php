<div>
    <p class="text-navy-400">{{ __('admin.credential.username_label') }}</p>
    <p class="font-mono font-semibold text-navy">{{ $username }}</p>
</div>
<div>
    <p class="text-navy-400">{{ __('admin.credential.password_label') }}</p>
    <p class="font-mono text-lg font-semibold text-navy" x-ref="passwordText">{{ $password }}</p>
</div>
