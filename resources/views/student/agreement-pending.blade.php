<x-guest-layout>
    <h1 class="mb-2 text-center text-lg font-bold text-navy">{{ __('student.agreement_pending.title') }}</h1>
    <p class="text-center text-sm text-navy-400">{{ __('student.agreement_pending.body') }}</p>
</x-guest-layout>
