/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
.claude/

# Source font folder — not part of the app. Only licence-recorded faces are
# committed, under resources/fonts/. See FONT-LICENCE.md.
/thaana

# Customer photo originals — kept out of the repo by request (23 MB of phone
# PNGs). Only the derived web renditions are committed, under
# public/images/photos/. Regenerating them needs the originals back from the
# customer. See CLAUDE.md § Photography.
/client photos/
